\ProvidesPackage{magnastavitve}
%
\usepackage[utf8x]{inputenc}   % omogoča uporabo slovenskih črk kodiranih v formatu UTF-8
\usepackage[slovene,english]{babel}    % naloži, med drugim, slovenske delilne vzorce
\usepackage[pdftex]{graphicx}  % omogoča vlaganje slik različnih formatov
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{fancyhdr}          % poskrbi, na primer, za glave strani
\usepackage{comment}			% doda dodatne opcije za komentarje, npr. okolje za večvrstične komentarje
% opisano v : http://texdoc.net/texmf-dist/doc/latex/comment/comment.pdf
\usepackage[pdftex, colorlinks=true,
						citecolor=black, filecolor=black, 
						linkcolor=black, urlcolor=black,
						pagebackref=false, 
                        pdfproducer={LaTeX}, pdfcreator={LaTeX},
                        unicode=true, hidelinks]{hyperref}
\usepackage{color}       % dodal Solina
%\usepackage{soul}
\usepackage{amsthm} % the backend package for theorem enviroments
\usepackage{amsmath} % package for math things
\usepackage{amsfonts} % math fonts
\usepackage{amssymb}
\usepackage{mathtools}
\usepackage{thmtools} % extends the theorem enviroment functionality
\usepackage{enumitem} % for enumeration enviroments
%\usepackage{commath} % ta paket povzroča problem pri uporabi dvopičja v tikzcd diagramih
\usepackage{array,xparse}
\usepackage{bm} % to make greek symbols bold
%
\usepackage{ulem} % ta knjižnica redefinira komando \emph
% ta komanda jo nastavi nazaj na privzeto vrednost
\normalem
%
\usepackage{tikz-cd} 
\usetikzlibrary{babel} % this fixes problems with tikz-cd
% http://tex.stackexchange.com/questions/166772/problem-with-babel-and-tikz-using-draw
\usepackage{bussproofs} % package for writing natural deduction tree-style proofs
%
\usepackage{listings} % paket za prikazovanje kode

\usepackage[raggedright]{titlesec} % da naslovi ne gredo v novo vrstico
% https://tex.stackexchange.com/questions/24777/how-to-disable-hyphenation-in-all-section-and-subsection-titles


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%	DIPLOMA INFO
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\newcommand{\ttitle}{Algebrajska in regularna kategorna logika}
\newcommand{\ttitleEn}{Algebraic and regular categorical logic}
\newcommand{\tsubject}{\ttitle}
\newcommand{\tsubjectEn}{\ttitleEn}
\newcommand{\tauthor}{Jure Taslak}
\newcommand{\tkeywords}{Teorija kategorij, Kategorična logika, kategorije, logika, algebrajske teorije, regularna logika}
\newcommand{\tkeywordsEn}{Category theory, Categorical logic, categories, logic, algebraic theories, regular logic}
%
%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% naslovi
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%  
\newcommand{\autfont}{\Large}
\newcommand{\titfont}{\LARGE\bf}
\newcommand{\clearemptydoublepage}{\newpage{\pagestyle{empty}\cleardoublepage}}
\setcounter{tocdepth}{1}
%
%

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% Postavitev strani                             %
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%
% with this we ensure that the chapter and section
% headings are in lowercase.
\renewcommand{\chaptermark}[1]{\markboth{#1}{}}
\renewcommand{\sectionmark}[1]{\markright{\thesection\ #1}}
\fancyhf{} % delete current setting for header and footer
\fancyhead[LE,RO]{\thepage}
\fancyhead[LO]{\rightmark}
\fancyhead[RE]{\leftmark}
\fancyhead[LE]{\thepage}
\fancyhead[RO]{\thepage}
\renewcommand{\headrulewidth}{0.5pt}
\renewcommand{\footrulewidth}{0pt}
\addtolength{\headheight}{3pt} % make space for the rule
\fancypagestyle{plain}{%
\fancyhead{} % get rid of headers on plain pages
\renewcommand{\headrulewidth}{0pt} % and the line
}
%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%									%
% konstrukti						%
%									%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%  
%
\theoremstyle{definition}
\newtheorem{definicija}{Definicija}[chapter]
\newtheorem{primer}[definicija]{Primer}
\newtheorem*{primer*}{Primer}
\theoremstyle{theorem}
\newtheorem{izrek}[definicija]{Izrek}
\newtheorem{trditev}[definicija]{Trditev}
\newtheorem{posledica}[definicija]{Posledica}
\newtheorem{lema}[definicija]{Lema}
\newenvironment{dokaz}{\begin{proof}[Dokaz]\ }{\end{proof}}
%
%
\theoremstyle{remark}
\newtheorem*{opomba}{Opomba}
%
\newenvironment{diagram}
{\begin{equation*}
  \begin{tikzcd}}
{\end{tikzcd}
\end{equation*}}

%

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%								%
%	Tikz-cd nastavitve			%
%								%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%
\tikzcdset{
 	diagrams={sep=large},
	labels={font=\small}
}
%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% Avtor in naslov			%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%
\author{Jure Taslak}
\title{Kategorična logika}
%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% pdfInfo
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%  
\pdfinfo{%
    /Title    (\ttitle)
    /Author   (\tauthor, jure.taslak@gmail.com)
    /Subject  (\ttitleEn)
    /Keywords (\tkeywordsEn)
    /ModDate  (\pdfcreationdate)
    /Trapped  /False
}
%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%									%
% 		Misc						%
%									%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%
\newcommand{\interp}[2][M]{{#2}^{(#1)}} % interpretacija argumenta v modelu M v regularni kategoriji
\newcommand{\caninterp}[1]{\interp[\mathcal{U}]{#1}} % interpretacija v kanoničnem modelu
%
\newcommand{\eqtext}[1]{\stackrel{\mathclap{\normalfont\mbox{#1}}}{=}} % write text over =
% could use a way to make the text smaller
%
\newcommand{\cat}[1]{{\operatorname{\mathbf{#1}}}} % za pisanje imen kategorij kot so Set in podobno
\newcommand{\homset}[2]{\mathrm{Hom(#1,#2)}}
%
% \DeclareMathOperator{\Hom}{Hom}
\newcommand{\Hom}{\mathbf{Hom}}
%\DeclareMathOperator[1]{\colim}{\underset{#1}{colim}}
\DeclareMathOperator{\colim}{colim}
%\newcommand[1]{\colim}{\underset{#1}\colim_op}
\DeclareMathOperator{\Fun}{Fun}
\DeclareMathOperator{\dom}{dom}
\DeclareMathOperator{\cod}{cod}
\DeclareMathOperator{\obj}{obj}
% \DeclareMathOperator{\Mod}{Mod}
\newcommand{\Mod}{\mathbf{Mod}}
\DeclareMathOperator{\Sub}{Sub}
\DeclareMathOperator{\graf}{graph}
\newcommand{\T}{\mathbb{T}}
%
\newcommand{\set}[1]{\lbrace\,#1\,\rbrace}
%
\newcommand{\setb}[2]{\left\lbrace\, #1 \,\middle|\, #2 \,\right\rbrace}
%
\newcommand{\fprod}[1]{\langle #1 \rangle}
%
\newcommand{\predsnop}[1]{\cat{Set}^{\cat{#1}^{op}}}
%
\newcommand{\powerset}[1]{\mathcal{P}(#1)}
%
%\DeclareMathOperator{\eval}{eval}
%
% \newcommand{\coprod}[2]{\cat{#1} + \cat{#2}}
%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%